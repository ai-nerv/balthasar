[package]
name = "balthasar-ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "balthasar's socket: the family's framing, and who is on the other end"

[dependencies]
ciborium.workspace = true
libc.workspace = true
rustix = { workspace = true }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
# For `Scratch` in the corpse-sweep tests: a directory that removes itself on a failing assertion
# as well as a passing one. `balthasar-model` is a leaf with three dependencies of its own, so
# this adds nothing to the build and cannot make a cycle -- nothing in it knows about a socket.
balthasar-model.workspace = true

[lints]
workspace = true
